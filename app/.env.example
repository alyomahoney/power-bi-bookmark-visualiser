# Plausible Analytics domain
# Set to your Plausible site domain (no https:// prefix, e.g. yourdomain.com)
# Read by src/shared/utils/telemetry.ts via import.meta.env.VITE_PLAUSIBLE_DOMAIN
# Leave empty (VITE_PLAUSIBLE_DOMAIN=) to disable telemetry
VITE_PLAUSIBLE_DOMAIN=
